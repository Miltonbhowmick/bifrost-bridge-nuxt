import {} from "~/utils/store/getter.names";
import {} from "~/utils/store/action.names";
import {} from "~/utils/store/endpoints";
import {} from "~/utils/store/mutation.names";
import { namespaced, buildParams } from "~/utils/utils";
import {} from "~/utils/store/namespace.names";

export const state = () => ({});

export const getters = {
	// [GET_VMT_TO_USD](state) {
	// 	return state.vmtToUsd;
	// },
};

export const actions = {
	// async [FETCH_NEWS]({ commit, dispatch }, payload) {
	// 	return new Promise((resolve, reject) => {
	// 		this.$axios
	// 			.get(`${NEWS_ENDPOINT}/?limit=10&offset=0`)
	// 			.then(({ data }) => {
	// 				// commit(SET_NEWS, data);
	// 				resolve(data);
	// 			})
	// 			.catch((e) => {
	// 				console.log(e);
	// 				reject(e);
	// 			});
	// 	});
	// },
};

export const mutations = {
	// [SET_CURRENCY_RATE](state, data) {
	// 	state.vmtToUsd = data["VMT"]["usd"];
	// 	state.omiToUsd = data["OMI"]["usd"];
	// },
};
